we create a container from a docker image

when i run docker run, instance of this image is created, which is a docker container

for example python:3.13.11, python is the image name, behind is the tag, together it forms a docker image